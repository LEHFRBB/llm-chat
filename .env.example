SECRET_KEY=change-me-to-a-random-string
DATABASE_URL=postgresql://postgres:postgres@localhost/chatapp
REDIS_URL=redis://localhost:6379/0
BASE_URL=http://127.0.0.1:8000

# Optional — GitHub OAuth (leave blank to disable)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Optional — path to a .gguf model file for local LLM inference
# If omitted and model.gguf exists at project root, it is used automatically
MODEL_PATH=
